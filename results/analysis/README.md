# 分析材料

此目录保存从 `results/final/` 派生的报告用表格、失败案例和历史记录。

- `baselines/`：基线指标导出与失败分析。
- `agent/`：Router / Agent 紧凑结果表，以及重组前冻结包的快照和 manifest。

`agent/` 中带有 `snapshot`、`original-layout` 或历史工作目录的文件用于追溯原始冻结包，因此可能保留旧路径；当前可运行路径以仓库根目录 `README.md` 为准。
